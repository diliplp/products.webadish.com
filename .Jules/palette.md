## 2024-04-16 - Add missing basic interactive states
**Learning:** This static site lacked basic interactive states for buttons (`cursor: pointer`, `disabled` opacity/cursor changes, and `focus-visible` outlines for keyboard navigation accessibility). Adding these baseline accessibility rules broadly affects the whole UI and provides immediate interactive feedback to the user and screen reader/keyboard users.
**Action:** Ensure these base interaction styles exist globally across all products.
